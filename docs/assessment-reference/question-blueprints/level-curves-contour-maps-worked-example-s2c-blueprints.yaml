schemaVersion: 1
assessmentId: level-curves-contour-maps-worked-example-s2c
categoryId: calculus-3
topicId: calc3-functions-of-several-variables
sourceId: src-20260719182540-a40fdcd443
packetId: packet-calc3-level-curves-contour-maps-v1
reviewState: approved
blueprints:
- questionId: s001
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: What is the temperature at B?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-s001
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: s002
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: A lies between which adjacent values, and what is its best midpoint estimate?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-s002
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: s003
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: Using those readings, how much warmer is B than A?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-s003
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: s004
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: From A, which way on the map leads toward larger contour values?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-s004
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: s005
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: Where do equal temperature changes occur over the shortest map distance?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-s005
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: s006
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: What equation describes the contour labeled 16°C on this map?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-s006
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex01
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: At D, halfway between 300 m and 350 m, what elevation should you estimate?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex01
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex02
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: E is on the 200-m contour. Estimate the signed elevation change from D to E.
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex02
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex03
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: Where is a 50-m elevation change packed into less horizontal distance?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex03
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex04
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: What equation describes the 300-m contour for elevation H?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex04
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex05
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: N lies on the 0°C isotherm. What is the temperature at N?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex05
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex06
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: M is on the 4°C contour and N is on the 0°C contour. What is $T(N)-T(M)$?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex06
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex07
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: From the 0°C contour at N, which direction leads toward increasing temperature?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex07
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
- questionId: ex08
  objectiveId: calc3-contour-map-interpretation
  sourceChunks:
  - chunk-2434
  - chunk-2436
  - chunk-2437
  questionType: multipleChoice
  givens: If M lies on the 4°C isotherm, which equation describes that curve?
  unknown: The map-based interpretation or estimate asked for in this prompt.
  representationRequirement: An original contour map with labeled function values, coordinate axes, and any relevant
    marked locations.
  governingPrinciple: A contour is the level set f(x,y)=c; comparing equal value intervals across distance reveals
    local change rate.
  methodSteps:
  - Identify the relevant labeled contour(s) and point position(s).
  - Interpolate between adjacent levels or compare equal-level spacing when needed.
  - Translate the result into a function value, signed change, equation, or geometric interpretation.
  likelyMisconception: contour-label-misread
  issueSignal: contour-label-misread
  difficultyTier: easy
  difficultyDimensions:
  - representationTransfer
  - estimationOrBounds
  subjectDifficultyTags:
  - contour-map-interpretation
  difficultyEvidence: Read the labeled contour geometry, identify the requested quantity, and verify the interpretation
    against neighboring levels.
  prerequisiteObjectives:
  - calc3-multivariable-representations
  extensionObjectives:
  - calc3-gradient-geometry
  semanticVariationAxes:
  - scenario
  - map geometry
  - unknown
  - method branch
  reasoningSignature: level-curves-contour-maps-worked-example-s2c-ex08
  answerVerificationMethod: Check the contour labels and point locations against the original SVG; independently
    recompute interpolation, signed change, or rate comparison.
  uniquenessEvidence: This item has its own labeled SVG, scenario, target quantity, and reasoning operation; its
    key map reading was checked against the plotted point and adjacent contour values.
  reviewState: approved
