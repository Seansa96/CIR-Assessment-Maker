schemaVersion: 1
id: cpp-intro-loops-worked-example
title: C++ Intro to Loops Worked Example
assessmentType: workedExample
categoryId: c++
topicId: c++-execution-flow
navigation:
  learningGoal: learn
  activityType: guidedWorkedExample
  tags:
  - c++
  - c++-execution-flow
  - cpp
  - c-plus-plus
  - workedexample
  - learn
  - guidedworkedexample
  - intro
  - loops
  - worked
  - example
skills:
- Apply Execution Flow in C++
- cpp
- c-plus-plus
- c++-execution-flow
- cpp-intro-loops
modeDefault: practice
randomizeQuestions: false
workedExamples:
- id: we001
  title: Building a counted loop in C++
  problem: Write a C++ function that uses a loop to add the integers from $1$ through
    $n$.
  steps:
  - id: step-we001-1
    title: Identify the repeated action
    instruction: Start by deciding what operation must happen more than once.
    type: multipleChoice
    prompt: In a function that adds the integers from $1$ through $n$, what is the
      repeated action?
    choices:
    - id: a
      text: Add the current counter value to a running total.
    - id: b
      text: Return from the function before the loop starts.
    - id: c
      text: Declare a new function on every iteration.
    - id: d
      text: Change the function name each time the loop runs.
    answer:
      choiceId: a
    hint: Think about what changes each time through the loop.
    explanation: |
      The loop should keep a running total and add the current counter value on each iteration.

      Related topic: Loop accumulation patterns in C++.

      Read further: Study `for` loops, loop counters, and accumulator variables in a C++ control-flow chapter.
  - id: step-we001-2
    title: Choose the loop bounds
    instruction: A counted loop needs a starting value, a stopping condition, and
      an update.
    type: multipleChoice
    prompt: Which loop header visits every integer from $1$ through $n$ exactly once?
    choices:
    - id: a
      text: '`for (int i = 1; i <= n; i++)`'
    - id: b
      text: '`for (int i = 1; i < n; i++)`'
    - id: c
      text: '`for (int i = n; i <= 1; i++)`'
    - id: d
      text: '`for (int i = 0; i < n; i--)`'
    answer:
      choiceId: a
    hint: The last value included should be exactly `n`.
    explanation: |
      Starting at `1`, continuing while `i <= n`, and incrementing with `i++` includes both endpoints.

      Related topic: Inclusive loop bounds and off-by-one errors.

      Read further: Review counted `for` loops and compare `<` versus `<=` in loop conditions.
  - id: step-we001-3
    title: Initialize the accumulator
    instruction: The running total must start from the additive identity.
    type: multipleChoice
    prompt: Before the loop starts, what value should `total` have?
    choices:
    - id: a
      text: '`0`'
    - id: b
      text: '`1`'
    - id: c
      text: '`n`'
    - id: d
      text: '`i`'
    answer:
      choiceId: a
    hint: Adding zero does not change the total.
    explanation: |
      An accumulator for addition should start at `0`; then each loop iteration adds one new value.

      Related topic: Accumulator initialization.

      Read further: Look up identity values for accumulation, especially `0` for sums and `1` for products.
  - id: step-we001-4
    title: Update inside the loop
    instruction: Now decide what statement belongs inside the loop body.
    type: multipleChoice
    prompt: Which statement correctly updates the accumulator?
    choices:
    - id: a
      text: '`total += i;`'
    - id: b
      text: '`i += total;`'
    - id: c
      text: '`total = n;`'
    - id: d
      text: '`return total;`'
    answer:
      choiceId: a
    hint: The accumulator should change, not the loop bound.
    explanation: |
      `total += i;` adds the current counter value to the running total.

      Related topic: Compound assignment in loop bodies.

      Read further: Review `+=` and\rhow loop body statements execute once per iteration.
  - id: step-we001-5
    title: Complete the function
    instruction: 'Put the loop together inside the full function definition.


      Submit the complete `int sumToN(int n)` function shown in the starter code.
      Keep the function name, return type, and parameter exactly the same. The app
      will call your function with test inputs, so do not write `main()`, do not use
      `cout`, and do not submit only the loop body.

      '
    type: code
    prompt: |
      Complete the C++ function so it returns the sum of the integers from `1` through `n`.

      Your answer should be the entire function definition:

      ```cpp
      int sumToN(int n) {
          ...
      }
      ```

      The test runner will call `sumToN(5)`, `sumToN(1)`, and `sumToN(10)` automatically.
    language: cpp
    functionName: sumToN
    starterCode: "int sumToN(int n) {\n    int total = 0;\n    // Add the integers\
      \ 1 through n.\n    return total;\n}\n"
    tests:
    - input: '5'
      expected: '15'
    - input: '1'
      expected: '1'
    - input: '10'
      expected: '55'
    answer: {}
    hint: Keep `int sumToN(int n)`, add a `for (int i = 1; i <= n; i++)` loop inside
      it, update `total`, and return `total` after the loop.
    explanation: |
      The submitted answer should be a complete function definition named `sumToN`. The runner compiles that submitted function and calls it with each test input.

      A correct solution initializes `total` to `0`, loops from `1` through `n`, adds each counter value, and returns the final total. Do not include `main()`, `cout`, or manual calls like `sumToN(5)`; the test harness handles those calls.

      Related topic: Complete counted-loop accumulation in C++.

      Read further: Practice writing functions that combine `for` loops, local variables, and return values.
