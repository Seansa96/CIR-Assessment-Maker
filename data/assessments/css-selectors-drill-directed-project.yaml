schemaVersion: 1
id: css-selectors-drill-directed-project
title: Directed Project - Specificity Drill
assessmentType: guidedProject
categoryId: css
subcategoryIds:
  - css-selectors-specificity
modeDefault: practice
guidedProject:
  language: css
  projectKind: console
  runnerMode: piston
  initialCode: |
    /* Write your CSS rules below */
    
    /* 1. Target the button inside the modal */
    
    
    /* 2. Target any paragraph immediately following a heading 2 */
    
    
    /* 3. Target links that have an active class inside the navigation */
    
  instructions: |
    In this drill, you will practice writing CSS selectors for specific scenarios.
    
    Write the 3 CSS selectors requested in the comments. You do not need to provide any properties inside the braces, just the selector and empty braces `{}`, like this:
    `selector {}`
