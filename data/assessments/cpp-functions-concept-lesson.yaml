schemaVersion: 1
id: cpp-functions-concept-lesson
title: C++ Functions Concept Lesson
assessmentType: conceptLesson
categoryId: c++
topicId: c++-functions
skills:
- Apply Functions in C++
- cpp
- c-plus-plus
- c++-functions
- cpp-functions
modeDefault: practice
randomizeQuestions: false
authoring:
  visualRequirement: notApplicable
  visualRationale: The lesson uses concrete C++ call and parameter code traces; no separate interface or spatial visual is needed.
navigation:
  learningGoal: learn
  activityType: conceptLesson
  tags:
  - c++
  - c++-functions
  - functions
  - parameters
  - return-types
  - cpp
  - c-plus-plus
  - conceptlesson
  - learn
  - concept
  - lesson
lesson:
  introduction: 'Functions allow you to organize code into reusable blocks. This lesson
    covers\rhow to define functions, the different ways to pass arguments, and\rhow
    C++ handles overloading and default parameters.

    '
  sections:
  - id: function-basics
    title: Signatures and Definitions
    required: true
    content: "A function has a return type, a name, and a parameter list.\n\n```cpp\n\
      // Declaration (often in a header file)\nint add(int a, int b);\n\n// Definition\
      \ (implementation)\nint add(int a, int b) {\n    return a + b;\n}\n```\n\nIf\
      \ a function doesn't return anything, its return type is `void`. You must declare\
      \ a function before you call it.\n"
    check:
      id: function-basics-check
      type: multipleChoice
      prompt: What is the return type of a function that simply prints a message and
        does not send a value back to the caller?
      choices:
      - id: a
        text: '`void`'
      - id: b
        text: '`int`'
      - id: c
        text: '`null`'
      - id: d
        text: '`string`'
      answer:
        choiceId: a
      explanation: '`void` indicates the function returns no value.

        '
  - id: parameter-passing
    title: Pass by Value vs. Reference
    required: true
    content: "By default, C++ passes arguments **by value** (makes a copy). Modifying\
      \ the copy doesn't affect the original.\n\nTo pass **by reference**, use an\
      \ ampersand `&`. This allows the function to modify the original variable.\n\
      \n```cpp\nvoid incrementCopy(int x) { x++; }   // Modifies a copy\nvoid incrementReal(int&\
      \ x) { x++; }  // Modifies the original\n```\n\nIf an object is large (like\
      \ a `std::string`) and you don't want to copy it or modify it, pass it by **const\
      \ reference**:\n\n```cpp\nvoid printName(const string& name) {\n    cout <<\
      \ name;\n}\n```\n"
    check:
      id: parameter-passing-check
      type: multipleChoice
      prompt: Which parameter type is best for a function that needs to read a large
        `std::vector` without copying it and without changing it?
      choices:
      - id: a
        text: '`const std::vector<int>&`'
      - id: b
        text: '`std::vector<int>`'
      - id: c
        text: '`std::vector<int>&`'
      - id: d
        text: '`std::vector<int>*`'
      answer:
        choiceId: a
      explanation: '`const &` avoids the expensive copy while guaranteeing the function
        cannot modify the vector.

        '
  - id: overloading
    title: Function Overloading
    required: true
    content: 'C++ allows multiple functions with the same name, as long as their parameter
      lists are different (different types or different number of parameters).


      ```cpp

      void print(int i) { cout << "Integer: " << i; }

      void print(double d) { cout << "Double: " << d; }

      void print(string s) { cout << "String: " << s; }

      ```


      The compiler decides which one to call based on the arguments provided. Overloading
      cannot be based solely on return type.

      '
    check:
      id: overloading-check
      type: multipleChoice
      prompt: Can you have `int getVal()` and `double getVal()` in the same scope?
      choices:
      - id: a
        text: No, because overloading requires different parameter lists, not just
          different return types.
      - id: b
        text: Yes, because the return types are different.
      - id: c
        text: Yes, but only if they are declared in separate files.
      - id: d
        text: No, because C++ does not support function overloading.
      answer:
        choiceId: a
      explanation: 'C++ cannot distinguish between calls based on what the caller
        *might* do with the return value, so parameter lists must differ.

        '
  - id: default-parameters
    title: Default Parameters
    required: true
    content: "You can assign default values to parameters. If the caller omits the\
      \ argument, the default is used.\n\n```cpp\nvoid greet(string name, string greeting\
      \ = \"Hello\") {\n    cout << greeting << \" \" << name;\n}\n\ngreet(\"Alice\"\
      );           // Prints: Hello Alice\ngreet(\"Bob\", \"Welcome\");  // Prints:\
      \ Welcome Bob\n```\n\nDefault parameters must be placed at the end of the parameter\
      \ list.\n"
    check:
      id: default-parameters-check
      type: multipleChoice
      prompt: Why is `void setSize(int width = 10, int height)` invalid?
      choices:
      - id: a
        text: Default parameters must appear at the end of the parameter list.
      - id: b
        text: Default parameters can only be used with strings.
      - id: c
        text: '`height` must also be 10 to match `width`.'
      - id: d
        text: It is actually perfectly valid.
      answer:
        choiceId: a
      explanation: 'Once a parameter has a default value, all subsequent parameters
        must also have default values.

        '
  - id: call-return-flow
    title: Trace Arguments, Parameters, and a Return Value
    required: true
    content: |
      At a call site, expressions are supplied as **arguments**. The function receives them through its **parameters**, performs its body, and may send one result back through `return`.

      ```cpp
      int square(int value) {
          return value * value;
      }

      int result = square(4); // argument 4; parameter value; result 16
      ```

      The parameter name exists only inside that function's scope; it is not a second name for the caller's variable.
    check:
      id: call-return-flow-check
      type: multipleChoice
      prompt: In `int result = square(4);`, what is `4`?
      choices: [{ id: a, text: An argument }, { id: b, text: The return type }, { id: c, text: A namespace }, { id: d, text: A header }]
      answer: { choiceId: a }
      explanation: The call supplies `4` as an argument; the function receives it through its parameter `value`.
  - id: declarations-and-definitions
    title: Separate a Function Contract from Its Body
    required: true
    content: |
      A declaration gives code enough information to call a function; a definition supplies the body. Keeping a declaration in a header and the definition in a source file lets several files use the same contract without duplicating its implementation.

      ```cpp
      // score.h
      int square(int value);

      // score.cpp
      int square(int value) { return value * value; }
      ```
    check:
      id: declarations-and-definitions-check
      type: multipleChoice
      prompt: Which part actually contains the statements a function executes?
      choices: [{ id: a, text: The definition }, { id: b, text: The declaration alone }, { id: c, text: The call argument }, { id: d, text: The namespace qualifier }]
      answer: { choiceId: a }
      explanation: A declaration states the function's signature; the definition includes the body that executes when called.
  - id: test-function-contracts
    title: Test the Contract at Its Boundary
    required: true
    content: |
      A small function is easier to test than a large `main`. Choose representative normal input and at least one boundary or failure case. A function that reports success with `bool` and writes a parsed result through a reference gives the caller a clear contract to test.
    check:
      id: test-function-contracts-check
      type: multipleChoice
      prompt: Which pair is a useful first test set for `bool get_number(stream, double&)`?
      choices: [{ id: a, text: One valid number and one invalid token }, { id: b, text: Two identical valid numbers only }, { id: c, text: A compiler version and a namespace }, { id: d, text: A header name and a return statement }]
      answer: { choiceId: a }
      explanation: The contract has two outcomes—success and failure—so both need an observable test before the helper is trusted.
