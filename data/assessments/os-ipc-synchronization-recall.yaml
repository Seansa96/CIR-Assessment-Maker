schemaVersion: 1
id: os-ipc-synchronization-recall
title: IPC and Synchronization Recall Drill
assessmentType: recallDrill
categoryId: operating-systems
subcategoryIds:
- os-ipc-synchronization
skills:
- Apply os-ipc-synchronization
modeDefault: practice
randomizeQuestions: true
navigation:
  learningGoal: recall
  activityType: mixedRecallSet
  tags:
  - operating-systems
  - os-ipc-synchronization
  - recall
items:
- id: race-condition-01
  explanation: race condition matters in IPC and Synchronization because it names a specific OS concept rather than a vague
    implementation detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - race-condition
  type: flashcard
  prompt: Recall the meaning of **race condition**.
  answer:
    expected: A bug where outcome depends on unpredictable timing.
- id: critical-region-02
  explanation: critical region matters in IPC and Synchronization because it names a specific OS concept rather than a vague
    implementation detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - critical-region
  type: cloze
  prompt: 'Complete the term: A section of code accessing shared state that needs protection. is called ____.'
  answer:
    expected: critical region
    aliases:
    - Critical Region
- id: mutex-03
  explanation: mutex matters in IPC and Synchronization because it names a specific OS concept rather than a vague implementation
    detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - mutex
  type: typed
  prompt: 'What OS term best matches this description?


    A lock used to provide mutual exclusion.'
  answer:
    expected: mutex
    aliases:
    - Mutex
- id: semaphore-04
  explanation: semaphore matters in IPC and Synchronization because it names a specific OS concept rather than a vague implementation
    detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - semaphore
  type: flashcard
  prompt: Recall the meaning of **semaphore**.
  answer:
    expected: A synchronization object with an atomic count.
- id: monitor-05
  explanation: monitor matters in IPC and Synchronization because it names a specific OS concept rather than a vague implementation
    detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - monitor
  type: cloze
  prompt: 'Complete the term: A synchronization construct combining state, procedures, and waiting conditions. is called ____.'
  answer:
    expected: monitor
    aliases:
    - Monitor
- id: barrier-06
  explanation: barrier matters in IPC and Synchronization because it names a specific OS concept rather than a vague implementation
    detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - barrier
  type: typed
  prompt: 'What OS term best matches this description?


    A synchronization point where participants wait until all have arrived.'
  answer:
    expected: barrier
    aliases:
    - Barrier
- id: message-passing-07
  explanation: message passing matters in IPC and Synchronization because it names a specific OS concept rather than a vague
    implementation detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - message-passing
  type: flashcard
  prompt: Recall the meaning of **message passing**.
  answer:
    expected: Communication by sending and receiving messages.
- id: busy-waiting-08
  explanation: busy waiting matters in IPC and Synchronization because it names a specific OS concept rather than a vague
    implementation detail.
  tags:
  - operating-systems
  - os-ipc-synchronization
  - busy-waiting
  type: cloze
  prompt: 'Complete the term: Repeatedly checking a condition while consuming CPU. is called ____.'
  answer:
    expected: busy waiting
    aliases:
    - Busy Waiting
