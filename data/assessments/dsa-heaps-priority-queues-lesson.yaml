schemaVersion: 1
id: dsa-heaps-priority-queues-lesson
categoryId: dsa
topicId: dsa-heaps-priority-queues
title: Introduction to Heaps
description: Understand the Min-Heap and Max-Heap properties.
navigation:
  learningGoal: learn
  activityType: conceptLesson
  tags:
  - heaps
  - priority-queue
  - trees
skills:
- 'id: obj-dsa-heaps-01: level: foundation'
blueprintId: bp-dsa-heaps-lesson-01
sourceChunkIds:
- src-20260810144616-0dc6e2835b:page-251-280
questions:
- id: intro-heaps
  type: freeResponse
  prompt: "**The Heap Property**\n\n![Min Heap to Array Diagram](/media/dsa/min-heap-array-mapping.svg)\n\
    \nA **Heap** is a specialized tree-based data structure that satisfies the heap property. \nIn a **Min-Heap**,\
    \ for any given node $C$, if $P$ is a parent node of $C$, then the value of $P$ is less than or equal\
    \ to the value of $C$. The root node is guaranteed to be the absolute smallest element.\nIn a **Max-Heap**,\
    \ the parent is always greater than or equal to its children, and the root is the absolute largest\
    \ element.\n\nUnlike a Binary Search Tree (where the left is smaller and the right is larger), a heap\
    \ makes no guarantees about the order of siblings. The left child could be larger than the right child,\
    \ or vice versa. The only rule is vertical (parent to child).\n\nBecause of this property, a Heap\
    \ is the ideal data structure to implement a **Priority Queue**, where you frequently need to extract\
    \ the \"highest priority\" (smallest or largest) element in $O(\\log N)$ time.\n\nIf you have a Min-Heap\
    \ and you want to find the *maximum* value in it, how long would it take in the worst case (in Big-O\
    \ notation), and why?\n"
  answer:
    gradingMode: selfCheck
  explanation: 'Solution:

    It would take $O(N)$ time.


    Why it works:

    Because a Min-Heap only guarantees that parents are smaller than children, the maximum value could
    be *any* of the leaf nodes at the bottom of the tree. Since there is no horizontal ordering, you would
    have to inspect all the leaf nodes (which make up roughly half the tree) to find the maximum, resulting
    in $O(N)$ time.

    '
