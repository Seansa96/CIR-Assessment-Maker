*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acid-base-reactions-easy-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acid-base-reactions-easy-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-acid-base-reactions-easy-quiz-s2c
+title: chemistry-acid-base-reactions easy quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-acid-base-reactions
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: easy
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: Which net ionic equation represents strong acid–strong hydroxide neutralization?
+  choices:
+  - id: a
+    text: H+ + OH− → H2O
+    issueSignals: []
+  - id: b
+    text: H+ + Cl− → HCl
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Na+ + OH− → NaOH
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: d
+    text: H2O → H+ + OH−
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from neutralization pattern and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Neutralization pattern: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-2
+  type: multipleChoice
+  prompt: In CH3COOH + NaOH neutralization, which remains molecular?
+  choices:
+  - id: a
+    text: NaOH
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: Na+ (unchanged spectator)
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: c
+    text: CH3COOH
+    issueSignals: []
+  - id: d
+    text: OH− (reactive hydroxide)
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from molecular to ionic and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Molecular to ionic: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-3
+  type: multipleChoice
+  prompt: Which ion is a spectator in HCl(aq) + NaOH(aq)?
+  choices:
+  - id: a
+    text: H+
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: OH− ion
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: c
+    text: Na+
+    issueSignals: []
+  - id: d
+    text: H2O
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from spectator ions and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Spectator ions: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-4
+  type: multipleChoice
+  prompt: What gas forms when Mg reacts with excess HCl?
+  choices:
+  - id: a
+    text: O2
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: H2
+    issueSignals: []
+  - id: c
+    text: Cl2
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: d
+    text: CO2
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from acid plus metal and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Acid plus metal: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-5
+  type: multipleChoice
+  prompt: What additional product distinguishes acid–carbonate reaction from hydroxide neutralization?
+  choices:
+  - id: a
+    text: H2 gas
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: CO2 gas
+    issueSignals: []
+  - id: c
+    text: O2 gas
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: d
+    text: NH3 gas
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from acid plus carbonate and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Acid plus carbonate: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-6
+  type: multipleChoice
+  prompt: How many moles of NaOH neutralize 0.020 mol H2SO4?
+  choices:
+  - id: a
+    text: 0.010 mol
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: b
+    text: 0.020 mol
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: c
+    text: 0.040 mol
+    issueSignals: []
+  - id: d
+    text: 0.200 mol
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from stoichiometric mole map and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Stoichiometric mole map: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-7
+  type: multipleChoice
+  prompt: For a strong acid reacting with water, which direction is favored?
+  choices:
+  - id: a
+    text: Toward hydronium and the conjugate base
+    issueSignals: []
+  - id: b
+    text: Toward undissociated strong acid only
+    issueSignals:
+    - id: acid-base-equilibrium-direction-error
+      domains:
+      - chemistry
+  - id: c
+    text: No reaction is possible
+    issueSignals:
+    - id: acid-base-equilibrium-direction-error
+      domains:
+      - chemistry
+  - id: d
+    text: Toward hydroxide formation
+    issueSignals:
+    - id: acid-base-equilibrium-direction-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from reaction direction and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Reaction direction: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acid base reactions to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acid base reactions to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acid base reactions to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
*** End Patch
*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acid-base-reactions-hard-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acid-base-reactions-hard-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-acid-base-reactions-hard-quiz-s2c
+title: chemistry-acid-base-reactions hard quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-acid-base-reactions
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: hard
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: Which net ionic equation represents strong acid–strong hydroxide neutralization?
+  choices:
+  - id: a
+    text: H+ + OH− → H2O
+    issueSignals: []
+  - id: b
+    text: H+ + Cl− → HCl
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Na+ + OH− → NaOH
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: d
+    text: H2O → H+ + OH−
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from neutralization pattern and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Neutralization pattern: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-2
+  type: multipleChoice
+  prompt: In CH3COOH + NaOH neutralization, which remains molecular?
+  choices:
+  - id: a
+    text: NaOH
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: Na+ (unchanged spectator)
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: c
+    text: CH3COOH
+    issueSignals: []
+  - id: d
+    text: OH− (reactive hydroxide)
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from molecular to ionic and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Molecular to ionic: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-3
+  type: multipleChoice
+  prompt: Which ion is a spectator in HCl(aq) + NaOH(aq)?
+  choices:
+  - id: a
+    text: H+
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: OH− ion
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  - id: c
+    text: Na+
+    issueSignals: []
+  - id: d
+    text: H2O
+    issueSignals:
+    - id: spectator-ion-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from spectator ions and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Spectator ions: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-4
+  type: multipleChoice
+  prompt: What gas forms when Mg reacts with excess HCl?
+  choices:
+  - id: a
+    text: O2
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: H2
+    issueSignals: []
+  - id: c
+    text: Cl2
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: d
+    text: CO2
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from acid plus metal and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Acid plus metal: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-5
+  type: multipleChoice
+  prompt: What additional product distinguishes acid–carbonate reaction from hydroxide neutralization?
+  choices:
+  - id: a
+    text: H2 gas
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: b
+    text: CO2 gas
+    issueSignals: []
+  - id: c
+    text: O2 gas
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  - id: d
+    text: NH3 gas
+    issueSignals:
+    - id: reaction-product-analysis-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from acid plus carbonate and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Acid plus carbonate: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-6
+  type: multipleChoice
+  prompt: How many moles of NaOH neutralize 0.020 mol H2SO4?
+  choices:
+  - id: a
+    text: 0.010 mol
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: b
+    text: 0.020 mol
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: c
+    text: 0.040 mol
+    issueSignals: []
+  - id: d
+    text: 0.200 mol
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from stoichiometric mole map and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Stoichiometric mole map: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-7
+  type: multipleChoice
+  prompt: For a strong acid reacting with water, which direction is favored?
+  choices:
+  - id: a
+    text: Toward hydronium and the conjugate base
+    issueSignals: []
+  - id: b
+    text: Toward undissociated strong acid only
+    issueSignals:
+    - id: acid-base-equilibrium-direction-error
+      domains:
+      - chemistry
+  - id: c
+    text: No reaction is possible
+    issueSignals:
+    - id: acid-base-equilibrium-direction-error
+      domains:
+      - chemistry
+  - id: d
+    text: Toward hydroxide formation
+    issueSignals:
+    - id: acid-base-equilibrium-direction-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from reaction direction and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Reaction direction: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acid base reactions to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acid base reactions to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acid base reactions to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
*** End Patch
*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acids-bases-easy-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acids-bases-easy-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-acids-bases-easy-quiz-s2c
+title: chemistry-acids-bases easy quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-acids-bases
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: easy
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: Which reaction is best classified with Brønsted–Lowry language without water?
+  choices:
+  - id: a
+    text: NH3 + HCl → NH4+ + Cl−
+    issueSignals: []
+  - id: b
+    text: NaOH → Na+ + OH− in water
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  - id: c
+    text: HCl(aq) → H+ + Cl−
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: KOH(aq) → K+ + OH−
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from model scope and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Model scope: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-2
+  type: multipleChoice
+  prompt: Which equation shows HCO3− acting as a base?
+  choices:
+  - id: a
+    text: HCO3− → CO3^2− + H+
+    issueSignals:
+    - id: amphiprotic-role-confusion
+      domains:
+      - chemistry
+  - id: b
+    text: HCO3− + H+ → H2CO3
+    issueSignals: []
+  - id: c
+    text: HCO3− → H+ + CO3^2− only
+    issueSignals:
+    - id: amphiprotic-role-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: HCO3− + OH− → H2O
+    issueSignals:
+    - id: amphiprotic-role-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from amphiprotic species and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Amphiprotic species: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-3
+  type: multipleChoice
+  prompt: Which acid is stronger at comparable conditions?
+  choices:
+  - id: a
+    text: Ka=1.0×10−3
+    issueSignals: []
+  - id: b
+    text: Ka=1.0×10−7
+    issueSignals:
+    - id: ka-expression-error
+      domains:
+      - chemistry
+  - id: c
+    text: They are equal
+    issueSignals:
+    - id: ka-expression-error
+      domains:
+      - chemistry
+  - id: d
+    text: Strength cannot be compared
+    issueSignals:
+    - id: ka-expression-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from ka and acid strength and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Ka and acid strength: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-4
+  type: multipleChoice
+  prompt: If Ka(HA)=1.0×10−5 at 25 °C, what is Kb(A−)?
+  choices:
+  - id: a
+    text: 1.0×10−9
+    issueSignals: []
+  - id: b
+    text: 1.0×10−5
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: c
+    text: 1.0×10−14
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: d
+    text: 1.0×10^9
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from kb and conjugates and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Kb and conjugates: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-5
+  type: multipleChoice
+  prompt: Which Ka expression matches a weak monoprotic acid?
+  choices:
+  - id: a
+    text: x^2/(C−x)
+    issueSignals: []
+  - id: b
+    text: C^2/x
+    issueSignals:
+    - id: chemistry-equilibrium-ice-stoichiometry-error
+      domains:
+      - chemistry
+  - id: c
+    text: (C−x)^2/x^2
+    issueSignals:
+    - id: chemistry-equilibrium-ice-stoichiometry-error
+      domains:
+      - chemistry
+  - id: d
+    text: x/(C+x)
+    issueSignals:
+    - id: chemistry-equilibrium-ice-stoichiometry-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from ice setup and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'ICE setup: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-6
+  type: multipleChoice
+  prompt: When is the weak-acid approximation least trustworthy?
+  choices:
+  - id: a
+    text: x/C=0.20
+    issueSignals: []
+  - id: b
+    text: x/C=0.002
+    issueSignals:
+    - id: chemistry-equilibrium-approximation-unchecked
+      domains:
+      - chemistry
+  - id: c
+    text: Ka is small and C moderate
+    issueSignals:
+    - id: chemistry-equilibrium-approximation-unchecked
+      domains:
+      - chemistry
+  - id: d
+    text: The acid is monoprotic
+    issueSignals:
+    - id: chemistry-equilibrium-approximation-unchecked
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from approximation check and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Approximation check: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-7
+  type: multipleChoice
+  prompt: Which statement is correct for a conjugate pair at 25 °C?
+  choices:
+  - id: a
+    text: KaKb=1.0×10−14
+    issueSignals: []
+  - id: b
+    text: Both members must be strong
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: c
+    text: Their concentrations are always equal
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: d
+    text: pKa and pKb are unrelated
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from conjugate equilibrium and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Conjugate equilibrium: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acids bases to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acids bases to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acids bases to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
*** End Patch
*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acids-bases-hard-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-acids-bases-hard-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-acids-bases-hard-quiz-s2c
+title: chemistry-acids-bases hard quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-acids-bases
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: hard
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: Which reaction is best classified with Brønsted–Lowry language without water?
+  choices:
+  - id: a
+    text: NH3 + HCl → NH4+ + Cl−
+    issueSignals: []
+  - id: b
+    text: NaOH → Na+ + OH− in water
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  - id: c
+    text: HCl(aq) → H+ + Cl−
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: KOH(aq) → K+ + OH−
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from model scope and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Model scope: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-2
+  type: multipleChoice
+  prompt: Which equation shows HCO3− acting as a base?
+  choices:
+  - id: a
+    text: HCO3− → CO3^2− + H+
+    issueSignals:
+    - id: amphiprotic-role-confusion
+      domains:
+      - chemistry
+  - id: b
+    text: HCO3− + H+ → H2CO3
+    issueSignals: []
+  - id: c
+    text: HCO3− → H+ + CO3^2− only
+    issueSignals:
+    - id: amphiprotic-role-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: HCO3− + OH− → H2O
+    issueSignals:
+    - id: amphiprotic-role-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from amphiprotic species and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Amphiprotic species: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-3
+  type: multipleChoice
+  prompt: Which acid is stronger at comparable conditions?
+  choices:
+  - id: a
+    text: Ka=1.0×10−3
+    issueSignals: []
+  - id: b
+    text: Ka=1.0×10−7
+    issueSignals:
+    - id: ka-expression-error
+      domains:
+      - chemistry
+  - id: c
+    text: They are equal
+    issueSignals:
+    - id: ka-expression-error
+      domains:
+      - chemistry
+  - id: d
+    text: Strength cannot be compared
+    issueSignals:
+    - id: ka-expression-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from ka and acid strength and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Ka and acid strength: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-4
+  type: multipleChoice
+  prompt: If Ka(HA)=1.0×10−5 at 25 °C, what is Kb(A−)?
+  choices:
+  - id: a
+    text: 1.0×10−9
+    issueSignals: []
+  - id: b
+    text: 1.0×10−5
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: c
+    text: 1.0×10−14
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: d
+    text: 1.0×10^9
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from kb and conjugates and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Kb and conjugates: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-5
+  type: multipleChoice
+  prompt: Which Ka expression matches a weak monoprotic acid?
+  choices:
+  - id: a
+    text: x^2/(C−x)
+    issueSignals: []
+  - id: b
+    text: C^2/x
+    issueSignals:
+    - id: chemistry-equilibrium-ice-stoichiometry-error
+      domains:
+      - chemistry
+  - id: c
+    text: (C−x)^2/x^2
+    issueSignals:
+    - id: chemistry-equilibrium-ice-stoichiometry-error
+      domains:
+      - chemistry
+  - id: d
+    text: x/(C+x)
+    issueSignals:
+    - id: chemistry-equilibrium-ice-stoichiometry-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from ice setup and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'ICE setup: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-6
+  type: multipleChoice
+  prompt: When is the weak-acid approximation least trustworthy?
+  choices:
+  - id: a
+    text: x/C=0.20
+    issueSignals: []
+  - id: b
+    text: x/C=0.002
+    issueSignals:
+    - id: chemistry-equilibrium-approximation-unchecked
+      domains:
+      - chemistry
+  - id: c
+    text: Ka is small and C moderate
+    issueSignals:
+    - id: chemistry-equilibrium-approximation-unchecked
+      domains:
+      - chemistry
+  - id: d
+    text: The acid is monoprotic
+    issueSignals:
+    - id: chemistry-equilibrium-approximation-unchecked
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from approximation check and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Approximation check: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-7
+  type: multipleChoice
+  prompt: Which statement is correct for a conjugate pair at 25 °C?
+  choices:
+  - id: a
+    text: KaKb=1.0×10−14
+    issueSignals: []
+  - id: b
+    text: Both members must be strong
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: c
+    text: Their concentrations are always equal
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  - id: d
+    text: pKa and pKb are unrelated
+    issueSignals:
+    - id: conjugate-constant-relation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from conjugate equilibrium and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Conjugate equilibrium: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acids bases to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acids bases to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry acids bases to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
*** End Patch
*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-ph-water-easy-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-ph-water-easy-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-ph-water-easy-quiz-s2c
+title: chemistry-ph-water easy quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-ph-water
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: easy
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: In pure water at 25 °C, which relation is true?
+  choices:
+  - id: a
+    text: '[H3O+]=[OH−]=1.0×10−7 M'
+    issueSignals: []
+  - id: b
+    text: '[H3O+]=1 M'
+    issueSignals:
+    - id: water-autoionization-error
+      domains:
+      - chemistry
+  - id: c
+    text: '[OH−]=0'
+    issueSignals:
+    - id: water-autoionization-error
+      domains:
+      - chemistry
+  - id: d
+    text: '[H3O+][OH−]=1'
+    issueSignals:
+    - id: water-autoionization-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from autoionization and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Autoionization: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-2
+  type: multipleChoice
+  prompt: What is pH when [H3O+]=1.0×10−3 M?
+  choices:
+  - id: a
+    text: '3.00'
+    issueSignals: []
+  - id: b
+    text: −3.00
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: c
+    text: '0.001'
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: d
+    text: pH cannot be negative
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from ph definition and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'pH definition: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-3
+  type: multipleChoice
+  prompt: A solution has pOH=3.00 at 25 °C. What is pH?
+  choices:
+  - id: a
+    text: pH remains 3.00
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: b
+    text: '7.00'
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: c
+    text: '11.00'
+    issueSignals: []
+  - id: d
+    text: '14.00'
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from poh and kw and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'pOH and Kw: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-4
+  type: multipleChoice
+  prompt: What is [H3O+] in 0.020 M HCl?
+  choices:
+  - id: a
+    text: 0.010 M
+    issueSignals:
+    - id: strong-acid-stoichiometry-error
+      domains:
+      - chemistry
+  - id: b
+    text: 0.020 M
+    issueSignals: []
+  - id: c
+    text: 0.040 M
+    issueSignals:
+    - id: strong-acid-stoichiometry-error
+      domains:
+      - chemistry
+  - id: d
+    text: 20 M
+    issueSignals:
+    - id: strong-acid-stoichiometry-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from strong-acid calculation and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Strong-acid calculation: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-5
+  type: multipleChoice
+  prompt: What distinguishes a weak-acid calculation?
+  choices:
+  - id: a
+    text: Include Ka and an equilibrium unknown
+    issueSignals: []
+  - id: b
+    text: Ignore acid concentration
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: c
+    text: Set dissociation to 100%
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Use pOH without finding OH−
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from weak-acid calculation and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Weak-acid calculation: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-6
+  type: multipleChoice
+  prompt: Compared with pH 5, how much more hydronium is at pH 3?
+  choices:
+  - id: a
+    text: 2 times
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: b
+    text: 10 times
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: c
+    text: 100 times
+    issueSignals: []
+  - id: d
+    text: 1000 times
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from log interpretation and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Log interpretation: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-7
+  type: multipleChoice
+  prompt: What follows from pH=9.20 at 25 °C?
+  choices:
+  - id: a
+    text: Acidic and [H3O+]>[OH−]
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: b
+    text: Basic and [OH−]>[H3O+]
+    issueSignals: []
+  - id: c
+    text: Neutral and equal concentrations
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: d
+    text: Impossible because pH cannot exceed 7
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from verification and transfer and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Verification and transfer: apply the governing acid-base relation and check the result against the
+    stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry ph water to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry ph water to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry ph water to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
*** End Patch
*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-ph-water-hard-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-ph-water-hard-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-ph-water-hard-quiz-s2c
+title: chemistry-ph-water hard quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-ph-water
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: hard
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: In pure water at 25 °C, which relation is true?
+  choices:
+  - id: a
+    text: '[H3O+]=[OH−]=1.0×10−7 M'
+    issueSignals: []
+  - id: b
+    text: '[H3O+]=1 M'
+    issueSignals:
+    - id: water-autoionization-error
+      domains:
+      - chemistry
+  - id: c
+    text: '[OH−]=0'
+    issueSignals:
+    - id: water-autoionization-error
+      domains:
+      - chemistry
+  - id: d
+    text: '[H3O+][OH−]=1'
+    issueSignals:
+    - id: water-autoionization-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from autoionization and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Autoionization: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-2
+  type: multipleChoice
+  prompt: What is pH when [H3O+]=1.0×10−3 M?
+  choices:
+  - id: a
+    text: '3.00'
+    issueSignals: []
+  - id: b
+    text: −3.00
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: c
+    text: '0.001'
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: d
+    text: pH cannot be negative
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from ph definition and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'pH definition: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-3
+  type: multipleChoice
+  prompt: A solution has pOH=3.00 at 25 °C. What is pH?
+  choices:
+  - id: a
+    text: pH remains 3.00
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: b
+    text: '7.00'
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  - id: c
+    text: '11.00'
+    issueSignals: []
+  - id: d
+    text: '14.00'
+    issueSignals:
+    - id: ph-poh-calculation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from poh and kw and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'pOH and Kw: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-4
+  type: multipleChoice
+  prompt: What is [H3O+] in 0.020 M HCl?
+  choices:
+  - id: a
+    text: 0.010 M
+    issueSignals:
+    - id: strong-acid-stoichiometry-error
+      domains:
+      - chemistry
+  - id: b
+    text: 0.020 M
+    issueSignals: []
+  - id: c
+    text: 0.040 M
+    issueSignals:
+    - id: strong-acid-stoichiometry-error
+      domains:
+      - chemistry
+  - id: d
+    text: 20 M
+    issueSignals:
+    - id: strong-acid-stoichiometry-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from strong-acid calculation and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Strong-acid calculation: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-5
+  type: multipleChoice
+  prompt: What distinguishes a weak-acid calculation?
+  choices:
+  - id: a
+    text: Include Ka and an equilibrium unknown
+    issueSignals: []
+  - id: b
+    text: Ignore acid concentration
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: c
+    text: Set dissociation to 100%
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Use pOH without finding OH−
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from weak-acid calculation and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Weak-acid calculation: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-6
+  type: multipleChoice
+  prompt: Compared with pH 5, how much more hydronium is at pH 3?
+  choices:
+  - id: a
+    text: 2 times
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: b
+    text: 10 times
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: c
+    text: 100 times
+    issueSignals: []
+  - id: d
+    text: 1000 times
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from log interpretation and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Log interpretation: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-7
+  type: multipleChoice
+  prompt: What follows from pH=9.20 at 25 °C?
+  choices:
+  - id: a
+    text: Acidic and [H3O+]>[OH−]
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: b
+    text: Basic and [OH−]>[H3O+]
+    issueSignals: []
+  - id: c
+    text: Neutral and equal concentrations
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  - id: d
+    text: Impossible because pH cannot exceed 7
+    issueSignals:
+    - id: ph-scale-interpretation-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from verification and transfer and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Verification and transfer: apply the governing acid-base relation and check the result against the
+    stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry ph water to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry ph water to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry ph water to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
*** End Patch
*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-titrations-easy-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-titrations-easy-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-titrations-easy-quiz-s2c
+title: chemistry-titrations easy quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-titrations
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: easy
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: Which distinction is correct?
+  choices:
+  - id: a
+    text: They are identical by definition
+    issueSignals:
+    - id: titration-equivalence-endpoint-confusion
+      domains:
+      - chemistry
+  - id: b
+    text: Equivalence is stoichiometric; endpoint is observed
+    issueSignals: []
+  - id: c
+    text: Endpoint is calculated from moles only
+    issueSignals:
+    - id: titration-equivalence-endpoint-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Equivalence depends on indicator color
+    issueSignals:
+    - id: titration-equivalence-endpoint-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from titration vocabulary and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Titration vocabulary: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-2
+  type: multipleChoice
+  prompt: For H2SO4 + 2NaOH at equivalence, moles NaOH are
+  choices:
+  - id: a
+    text: half the H2SO4 moles
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: b
+    text: equal to H2SO4 moles
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: c
+    text: twice the H2SO4 moles
+    issueSignals: []
+  - id: d
+    text: four times the H2SO4 moles
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from mole ratio and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Mole ratio: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-3
+  type: multipleChoice
+  prompt: 25.0 mL HCl is neutralized by 18.0 mL of 0.100 M NaOH. What is MHCl?
+  choices:
+  - id: a
+    text: 0.0180 M
+    issueSignals:
+    - id: titration-volume-concentration-error
+      domains:
+      - chemistry
+  - id: b
+    text: 0.0720 M
+    issueSignals: []
+  - id: c
+    text: 0.100 M
+    issueSignals:
+    - id: titration-volume-concentration-error
+      domains:
+      - chemistry
+  - id: d
+    text: 0.139 M
+    issueSignals:
+    - id: titration-volume-concentration-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from unknown concentration and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Unknown concentration: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-4
+  type: multipleChoice
+  prompt: Which indicator property matters most?
+  choices:
+  - id: a
+    text: Its range overlaps the steep equivalence region
+    issueSignals: []
+  - id: b
+    text: It is always colorless
+    issueSignals:
+    - id: indicator-range-selection-error
+      domains:
+      - chemistry
+  - id: c
+    text: Its molarity equals analyte
+    issueSignals:
+    - id: indicator-range-selection-error
+      domains:
+      - chemistry
+  - id: d
+    text: It reacts stoichiometrically
+    issueSignals:
+    - id: indicator-range-selection-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from indicator transition and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Indicator transition: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-5
+  type: multipleChoice
+  prompt: At half-equivalence for weak acid/strong base, which equality holds?
+  choices:
+  - id: a
+    text: pH=pKa
+    issueSignals: []
+  - id: b
+    text: pH=7 always
+    issueSignals:
+    - id: titration-region-misidentified
+      domains:
+      - chemistry
+  - id: c
+    text: '[HA]=0'
+    issueSignals:
+    - id: titration-region-misidentified
+      domains:
+      - chemistry
+  - id: d
+    text: pOH=pKa
+    issueSignals:
+    - id: titration-region-misidentified
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from curve regions and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Curve regions: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-6
+  type: multipleChoice
+  prompt: Why is that equivalence solution basic?
+  choices:
+  - id: a
+    text: The conjugate base hydrolyzes water
+    issueSignals: []
+  - id: b
+    text: Strong base remains unreacted
+    issueSignals:
+    - id: titration-equivalence-chemistry-error
+      domains:
+      - chemistry
+  - id: c
+    text: Indicator creates OH−
+    issueSignals:
+    - id: titration-equivalence-chemistry-error
+      domains:
+      - chemistry
+  - id: d
+    text: The acid becomes strong
+    issueSignals:
+    - id: titration-equivalence-chemistry-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from weak/strong distinction and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Weak/strong distinction: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q-7
+  type: multipleChoice
+  prompt: Which error makes recorded titrant volume too large?
+  choices:
+  - id: a
+    text: Adding titrant past the first persistent endpoint color
+    issueSignals: []
+  - id: b
+    text: Rinsing buret with titrant
+    issueSignals:
+    - id: titration-experimental-error
+      domains:
+      - chemistry
+  - id: c
+    text: Reading meniscus at eye level
+    issueSignals:
+    - id: titration-experimental-error
+      domains:
+      - chemistry
+  - id: d
+    text: Running a blank trial
+    issueSignals:
+    - id: titration-experimental-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from testing and error and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: 'Testing and error: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry titrations to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry titrations to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry titrations to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds: []
*** End Patch
*** Begin Patch
*** Delete File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-titrations-hard-quiz-s2c.yaml
*** End Patch
*** Begin Patch
*** Add File: C:/Users/SeanS/Downloads/cir_app/data/assessments/chemistry-titrations-hard-quiz-s2c.yaml
+schemaVersion: 1
+id: chemistry-titrations-hard-quiz-s2c
+title: chemistry-titrations hard quiz
+assessmentType: quiz
+categoryId: chemistry
+topicId: chemistry-titrations
+modeDefault: practice
+randomizeQuestions: true
+navigation:
+  learningGoal: practice
+  activityType: focusedPractice
+  tags:
+  - chemistry
+  - acid-base
+  - s2c-reviewed
+authoring:
+  visualRequirement: notApplicable
+  visualRationale: Self-contained scored chemistry questions.
+  difficultyTier: hard
+  sourceId: src-20260720035703-91ea51b0d8
+questions:
+- id: q-1
+  type: multipleChoice
+  prompt: Which distinction is correct?
+  choices:
+  - id: a
+    text: They are identical by definition
+    issueSignals:
+    - id: titration-equivalence-endpoint-confusion
+      domains:
+      - chemistry
+  - id: b
+    text: Equivalence is stoichiometric; endpoint is observed
+    issueSignals: []
+  - id: c
+    text: Endpoint is calculated from moles only
+    issueSignals:
+    - id: titration-equivalence-endpoint-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Equivalence depends on indicator color
+    issueSignals:
+    - id: titration-equivalence-endpoint-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from titration vocabulary and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Titration vocabulary: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-2
+  type: multipleChoice
+  prompt: For H2SO4 + 2NaOH at equivalence, moles NaOH are
+  choices:
+  - id: a
+    text: half the H2SO4 moles
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: b
+    text: equal to H2SO4 moles
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  - id: c
+    text: twice the H2SO4 moles
+    issueSignals: []
+  - id: d
+    text: four times the H2SO4 moles
+    issueSignals:
+    - id: stoichiometry-ratio-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: c
+  explanation: 'Solution: Choice C follows from mole ratio and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Mole ratio: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-3
+  type: multipleChoice
+  prompt: 25.0 mL HCl is neutralized by 18.0 mL of 0.100 M NaOH. What is MHCl?
+  choices:
+  - id: a
+    text: 0.0180 M
+    issueSignals:
+    - id: titration-volume-concentration-error
+      domains:
+      - chemistry
+  - id: b
+    text: 0.0720 M
+    issueSignals: []
+  - id: c
+    text: 0.100 M
+    issueSignals:
+    - id: titration-volume-concentration-error
+      domains:
+      - chemistry
+  - id: d
+    text: 0.139 M
+    issueSignals:
+    - id: titration-volume-concentration-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: b
+  explanation: 'Solution: Choice B follows from unknown concentration and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Unknown concentration: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-4
+  type: multipleChoice
+  prompt: Which indicator property matters most?
+  choices:
+  - id: a
+    text: Its range overlaps the steep equivalence region
+    issueSignals: []
+  - id: b
+    text: It is always colorless
+    issueSignals:
+    - id: indicator-range-selection-error
+      domains:
+      - chemistry
+  - id: c
+    text: Its molarity equals analyte
+    issueSignals:
+    - id: indicator-range-selection-error
+      domains:
+      - chemistry
+  - id: d
+    text: It reacts stoichiometrically
+    issueSignals:
+    - id: indicator-range-selection-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from indicator transition and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Indicator transition: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-5
+  type: multipleChoice
+  prompt: At half-equivalence for weak acid/strong base, which equality holds?
+  choices:
+  - id: a
+    text: pH=pKa
+    issueSignals: []
+  - id: b
+    text: pH=7 always
+    issueSignals:
+    - id: titration-region-misidentified
+      domains:
+      - chemistry
+  - id: c
+    text: '[HA]=0'
+    issueSignals:
+    - id: titration-region-misidentified
+      domains:
+      - chemistry
+  - id: d
+    text: pOH=pKa
+    issueSignals:
+    - id: titration-region-misidentified
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from curve regions and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Curve regions: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-6
+  type: multipleChoice
+  prompt: Why is that equivalence solution basic?
+  choices:
+  - id: a
+    text: The conjugate base hydrolyzes water
+    issueSignals: []
+  - id: b
+    text: Strong base remains unreacted
+    issueSignals:
+    - id: titration-equivalence-chemistry-error
+      domains:
+      - chemistry
+  - id: c
+    text: Indicator creates OH−
+    issueSignals:
+    - id: titration-equivalence-chemistry-error
+      domains:
+      - chemistry
+  - id: d
+    text: The acid becomes strong
+    issueSignals:
+    - id: titration-equivalence-chemistry-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from weak/strong distinction and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Weak/strong distinction: apply the governing acid-base relation and check the result against the stated
+    condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q-7
+  type: multipleChoice
+  prompt: Which error makes recorded titrant volume too large?
+  choices:
+  - id: a
+    text: Adding titrant past the first persistent endpoint color
+    issueSignals: []
+  - id: b
+    text: Rinsing buret with titrant
+    issueSignals:
+    - id: titration-experimental-error
+      domains:
+      - chemistry
+  - id: c
+    text: Reading meniscus at eye level
+    issueSignals:
+    - id: titration-experimental-error
+      domains:
+      - chemistry
+  - id: d
+    text: Running a blank trial
+    issueSignals:
+    - id: titration-experimental-error
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A follows from testing and error and the stated species or numerical condition.
+
+
+    Why it works: Apply the governing acid-base definition or equation while preserving proton bookkeeping, stoichiometry,
+    units, and assumptions.
+
+
+    Why the other choices fail: Each alternative makes a prompt-specific error such as reversing donor and acceptor roles,
+    using the wrong coefficient, treating a weak species as fully dissociated, or misreading pH or endpoint evidence.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: 'Testing and error: apply the governing acid-base relation and check the result against the stated condition.'
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q008
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry titrations to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q009
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry titrations to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
+- id: q0010
+  type: multipleChoice
+  prompt: Which verification step is essential when applying chemistry titrations to a new problem?
+  choices:
+  - id: a
+    text: Check the governing relation, units, and stated condition
+    issueSignals: []
+  - id: b
+    text: Ignore coefficients and signs
+    issueSignals:
+    - id: chemical-equation-balancing-error
+      domains:
+      - chemistry
+  - id: c
+    text: Assume every process is complete
+    issueSignals:
+    - id: strong-weak-acid-base-confusion
+      domains:
+      - chemistry
+  - id: d
+    text: Replace the prompt with a memorized example
+    issueSignals:
+    - id: acid-base-model-confusion
+      domains:
+      - chemistry
+  answer:
+    choiceId: a
+  explanation: 'Solution: Choice A. Why it works: An independent check verifies the model, units, and conditions. Why the
+    other choices fail: The alternatives discard coefficients, assumptions, or the actual prompt.'
+  difficultyDimensions:
+  - modelOrDerivation
+  - errorDiagnosis
+  - representationTransfer
+  difficultyEvidence: Transfers the topic method to a new condition and verifies units and assumptions.
+  prerequisiteObjectiveIds:
+  - acid-base-models
+  extensionObjectiveIds:
+  - acid-base-transfer
*** End Patch
